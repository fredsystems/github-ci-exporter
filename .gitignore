# Rust
target/
debug/
**/*.rs.bk
*.pdb

# Nix / direnv
.direnv/
result
result-*

# Generated by git-hooks.nix from the `checks` output in flake.nix; it is a
# symlink into /nix/store and differs per machine. flake.nix is the source of
# truth for the hook set.
.pre-commit-config.yaml

# Coverage
*.profraw
*.lcov
lcov.info

# Local runtime state (ETag cache, scratch config)
/state/
/config.local.toml

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
